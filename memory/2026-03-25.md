# 2026-03-25

- Martin request: Research model outputs should include per-race pace hint in format "步速提示 + 理由" (<=2 reasons). Reasons may mix pre-race explainable factors and internal model signals.
- Martin request: From now on, Production WQ prediction JSON output must use picks-only schema `productionWQ-picks.v2` (W pick + 2 Q partners, include overlay) at `prediction/<dd-mm-yyyy>/productionWQ-result.json`.
