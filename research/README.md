# Research Area

This folder is for experiments.

Rules:
- Do **not** edit `prod/HKJC_PROD_WQ.json` from research.
- Do **not** overwrite pinned production artifacts under `models/`.
- Put research models under `research/models/`.
- Put research reports under `research/reports/`.
- Any promotion to production must use `scripts/promote_to_prod.sh`.
